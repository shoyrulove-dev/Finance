import type { Metadata } from "next";
import { getDatabase } from "@/lib/mongodb";
import { formatCompact } from "@/lib/market-tools";
export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Prediction market dashboard", description: "Discover active prediction markets ranked by recent volume and liquidity.", alternates: { canonical: "/markets/prediction-markets" } };
export default async function PredictionMarketsPage() { const db = await getDatabase(); const rows = await db.collection("prediction_markets").find({ active: true }).sort({ volume24h: -1 }).limit(40).toArray(); return <main className="data-page tool-page"><div className="data-page-heading"><div><p className="eyebrow">MARKET THEME</p><h1>Prediction markets</h1><p>Active markets ranked by recent trading volume, with implied probabilities from market prices.</p></div></div><section className="prediction-grid">{rows.map((row: any) => { const yesIndex = Array.isArray(row.outcomes) ? row.outcomes.findIndex((item: string) => String(item).toLowerCase() === "yes") : -1; const probability = yesIndex >= 0 ? Number(row.prices?.[yesIndex]) * 100 : Number(row.prices?.[0]) * 100; return <a className="prediction-card" href={row.url} target="_blank" rel="noopener noreferrer" key={row.providerId}><span className="prediction-probability">{Number.isFinite(probability) ? `${probability.toFixed(1)}%` : "—"}</span><h2>{row.question}</h2><div><span>24h volume <strong>${formatCompact(Number(row.volume24h))}</strong></span><span>Liquidity <strong>${formatCompact(Number(row.liquidity))}</strong></span></div><small>View market ↗</small></a>; })}</section>{!rows.length && <div className="asset-panel"><p>This dashboard is being populated by the next scheduled sync.</p></div>}<p className="data-footnote">Probabilities are market prices, not forecasts by Bliss Finance. Availability and legal access vary by jurisdiction.</p></main>; }
